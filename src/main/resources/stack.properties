#default name
name=stack

# default version
version=0.0.1

# default port
port=5555

# required prefix (must be different)
api.prefix=api
docs.prefix=docs

# swagger configuration
swagger.title=stack-swagger-ui
swagger.description=endpoints of all resources

# dist folder from swagger-ui
swagger.dist.folder=swagger-ui